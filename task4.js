function bar (callback) {
     foo(function (err, data) {  
       if (err)
         return callback(err)


           for (i=0;i<x.length;i++){
             if(.search(title)>-1){
               console.log();
             }
           }


       callback(null, data)
     })
   }

function find()
